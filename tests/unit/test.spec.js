describe('test', () => {
    it('should work 1', () => {
        const a = 2 + 2
        expect(a).toBe(4)
    })
    it('should work 2', () => {
        const a = 2 + 1
        expect(a).toBe(3)
    })
})